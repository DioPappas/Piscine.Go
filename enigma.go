package piscine

func Enigma(a ***int, b *int, c *******int, d ****int) {
	// Temporary variables to hold intermediate values
	tempA := ***a
	tempB := *b
	tempC := *******c
	tempD := ****d

	// Assignments as per the specified rules
	*******c = tempA
	****d = tempC
	*b = tempD
	***a = tempB
}
